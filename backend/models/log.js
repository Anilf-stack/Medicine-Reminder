class Log {
    constructor(id, userId, medicineId, status, timestamp) {
        this.id = id;
        this.userId = userId;
        this.medicineId = medicineId;
        this.status = status;
        this.timestamp = timestamp;
    }
}

export default Log;
